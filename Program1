// Program 1
#include <iostream>
using namespace std;

int main()
{
    // Declaring the year and month variables with a default value.
    int year = 0;
    int month = 0;
    // Ask the user what year and month they'd like to measure.
    cout << "What year would you like to measure? \n";
    cin >> year;
    cout << "What month would you like to know the number of days of? (1-12)\n";
    cin >> month;
    // Detect if the month is a 30-day month, a 31-day month, or February.
    if (month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12) {
        cout << "31 days";
    }
    else if (month == 4 || month == 6 || month == 9 || month == 11) {
        cout << "30 days";
    }
    // If the user puts in February, go off of the year variable to determine 
    else if (month == 2) {
        if (year % 4 == 0) {
        if (year % 100 == 0) {
        if (year % 400 > 0) {
        cout << "28 days";
        }
        else {
            cout << "29 days";
        }
        }
        else {
           cout << "29 days"; 
        }
        }
        else {
            cout << "28 days";
        }
    }
    else {
        cout << "Invalid month.";
    }
    return 0;
}
